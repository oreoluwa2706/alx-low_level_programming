#include "main.h"

/**
 * _abs - Entry point of the code that compute the
 * @n: The chaaracter to check is n
 * Return: Always 0
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	return (-n);
}
