#include "main.h"

/**
 * main - Entry point of the code that compute the
 * absolute value of an integer
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	return (-n);
}
