#include "main.h"

/**
 * main - Entry point of a function that
 * add two integers and return the result
 * Return: Always 0 (Success)
 */
int add(int firstNum, int secondNum)
{
	int addBoth;

	addBoth = firstNum + secondNum;
	return (addBoth);
}
